{
  "name": "adaptive-node",
  "type": "module",
  "version": "1.0.3",
  "description": "adaptive node router",
  "main": "dist/core.js",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mvklingeren/adaptive-node-graph"
  },
  "keywords": [
    "adaptive",
    "node",
    "router"
  ],
  "scripts": {
    "build": "esbuild src/**/*.ts --bundle --platform=node --target=node18 --format=esm --outdir=dist --splitting --chunk-names=chunks/[name]-[hash]",
    "build:watch": "esbuild src/**/*.ts --bundle --platform=node --target=node18 --format=esm --outdir=dist --splitting --chunk-names=chunks/[name]-[hash] --watch",
    "test-basics": "esbuild src/test-basics.ts --bundle --platform=node --target=node18 --format=esm --outfile=dist/test-basics.js && node dist/test-basics.js",
    "test-improvements": "esbuild src/test-improvements.ts --bundle --platform=node --target=node18 --format=esm --outfile=dist/test-improvements.js && node dist/test-improvements.js",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mvklingeren",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "esbuild": "^0.25.5"
  }
}
